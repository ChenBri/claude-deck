pygame>=2.5
pytest>=8
pyyaml>=6
pyboy>=2.7  # Game Boy/GBC emulation, ui/scenes/gameboy.py; see docs/BUILD.md for the arm64 caveat
numpy  # audio buffer conversion in ui/scenes/gameboy.py; already a pyboy dependency, listed since we import it directly
